apiVersion: v1
kind: Service
metadata:
  name: xch-cms
  namespace: meta-cogni
  labels:
    app: xch-cms
    component: web
spec:
  type: ClusterIP
  selector:
    app: xch-cms
    component: web
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
